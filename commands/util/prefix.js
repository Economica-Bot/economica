module.exports = {
     aliases: ['prefix', 'getprefix'],
     description: 'Changes the server prefix to the first argument or returns the current prefix if left blank',
     callback: (message, arguments, text) => {
          
   ***REMOVED***
}