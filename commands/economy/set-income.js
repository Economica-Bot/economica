module.exports = {
     aliases: ['set-income'],
     expectedArgs: '<type | all> [min] [max]',
     exUse: 'work 0 500',
     callback: (message, arguments, text) => {
          message.channel.send('indev')
   ***REMOVED***
}